8. switch (item.value) {
        case 'square':
          square.finall(canvasBody);
          break;
        case 'triangle':
          triangle.finall(canvasBody);
          break;
        case 'circle':
          circle.finall(canvasBody);
          break;
        case 'rectangle':
          rectangle.finall(canvasBody);
          break;
      };
как можно это улучшить?
9. при добавлении нового итема не должно ничего удаляться/меняться
10. фигура не должна выходить за пределы блока отрисовки
